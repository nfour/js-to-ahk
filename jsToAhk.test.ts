describe('use cases', () => {
  function createDefaultUsecase () {
    
  }
  
  test('just the globals', async () => {
    compileTo
  })
  test('basic script, bindings', async () => {
    compileTo
  })
})